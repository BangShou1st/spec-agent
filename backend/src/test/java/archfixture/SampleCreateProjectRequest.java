package archfixture;

/**
 * HTTP-only DTO fixture: Request-suffixed and referenced by
 * {@link SampleProjectController}, so the HTTP DTO role picks it up.
 */
public class SampleCreateProjectRequest {
}
