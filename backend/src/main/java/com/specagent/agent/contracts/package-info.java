/**
 * Agent contracts: domain-neutral DTOs exchanged between the agent loop and
 * the model adapter. These types are stable, free of external SDKs, and carry
 * no concrete business-domain fields.
 */
package com.specagent.agent.contracts;