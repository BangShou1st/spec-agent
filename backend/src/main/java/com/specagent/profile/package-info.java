/**
 * Profile: generic requirement dimensions and output preferences.
 * Profiles are configuration, not code branches.
 * First version ships only 'generic_requirement' profile.
 */
package com.specagent.profile;