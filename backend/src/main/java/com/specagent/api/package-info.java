/**
 * API boundary for Spec Agent. Controllers and DTOs live here; they must go
 * through runtime services and never bypass the service layer, expose domain
 * entities directly, or touch model/provider/context/credential internals.
 */
package com.specagent.api;