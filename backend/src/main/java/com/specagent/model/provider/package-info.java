/**
 * ProviderAdapter: provider-specific HTTP protocol details.
 * Hides provider-specific request/response JSON behind stable contracts.
 * Must support configurable User-Agent (opencode/1.18.16 for opencode zen).
 * Must not contain requirement-domain behavior.
 */
package com.specagent.model.provider;